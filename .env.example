HOST=0.0.0.0
PORT=1337

# Secrets - GENERATE NEW VALUES FOR PRODUCTION
# Use: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
APP_KEYS="toBeModified1,toBeModified2"
API_TOKEN_SALT=tobemodified
ADMIN_JWT_SECRET=tobemodified
TRANSFER_TOKEN_SALT=tobemodified
JWT_SECRET=tobemodified

# Database - For local development (SQLite)
DATABASE_CLIENT=sqlite
DATABASE_FILENAME=.tmp/data.db

# Database - For production (PostgreSQL on Render)
# Uncomment and use these for Render deployment:
# DATABASE_CLIENT=postgres
# DATABASE_URL=your-postgres-connection-string-from-render
# DATABASE_SSL=false

# Server Configuration
NODE_ENV=development
PUBLIC_URL=http://localhost:1337

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,https://frontend-pink-two-50.vercel.app